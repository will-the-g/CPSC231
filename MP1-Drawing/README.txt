Will Gatlin
002427413
wgatlin@chapman.edu
CPSC231-03
MP1 - Drawing

class Drawing{
    createCanvas(){
        returns a 2D array with the empty canvas
    }
    printCanvas(){
        no returns
        parameters: takes in the canvas as a 2D array
    }
    rangeCheck(){
        returns: boolean telling if the int is in the right range
        parameters: int array of the coordinates
    }
    getCoordinates(){
        returns an int array to grab coordinates from the user
    }
    main(){
        no returns
        parameters: String array
    }
    



}